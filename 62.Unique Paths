class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        table = [[1]*n for _ in xrange(m)]
        for i in xrange(1,m):
            for j in xrange(1,n):
                table[i][j] = table[i-1][j] + table[i][j-1]
        return table[-1][-1]
  """
  DP
  """
